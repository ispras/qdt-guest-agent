# QDT Guest Agent

## Prerequisites

Needed:

  - scons build system.
  - A tool chain for cross-compilation of C/C++ for Windows 32-bit
    (e.g. MinGW).

### Ubuntu Linux

#### 18.04

```bash
sudo apt install mingw-w64
```

